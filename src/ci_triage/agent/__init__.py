"""LLM investigation agent: a LangGraph loop over failure-time evidence.

Receives the same `CaseView` as the rule baseline and returns the same `Diagnosis`, so
the evaluation harness scores both with one ruler.
"""
