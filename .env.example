# GitHub token used for read-only GitHub API calls (workflow runs, jobs, logs).
# A fine-grained PAT with public-repository read access is enough.
# Copy this file to .env and fill in the value. Never commit .env.
GITHUB_TOKEN=
